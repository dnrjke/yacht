{
  "name": "yacht-root",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "core"
  ],
  "scripts": {
    "build:server": "npm run build --workspace=core && npm run build --workspace=backend",
    "start:server": "npm run start --workspace=backend"
  },
  "overrides": {
    "three-mesh-bvh": "^0.8.0"
  }
}
